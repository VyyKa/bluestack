- `GET /healthz`: returns `ok`
- `GET /logcheck`: returns simple status counters (uptime / total requests / last request info)
- `PORT` (default: `9002`)
- `REDACT_SENSITIVE` (default: `true`) – redact `Cookie` / `Authorization` in output


